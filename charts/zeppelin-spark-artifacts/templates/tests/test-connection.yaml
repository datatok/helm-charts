apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zeppelin-spark-artifacts.fullname" . }}-test-connection"
  labels:
    {{- include "zeppelin-spark-artifacts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zeppelin-spark-artifacts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
