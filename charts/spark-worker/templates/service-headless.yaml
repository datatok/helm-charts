apiVersion: v1
kind: Service
metadata:
  name: {{ include "spark-worker.fullname" . }}-headless
  labels:
    {{- include "spark-worker.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    {{- include "spark-worker.selectorLabels" . | nindent 4 }}
